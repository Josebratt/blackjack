import { FaceCardsPipe } from './face-cards.pipe';

describe('FaceCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new FaceCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
